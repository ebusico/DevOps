---
- name: Update all modules
  apt:
     name: "*"
     state: latest
     force_apt_get: yes

- name: Upgrade all modules
  apt:
     upgrade: dist

- name: Install system dependencies
  apt:
     name: "{{ packages }}" 
